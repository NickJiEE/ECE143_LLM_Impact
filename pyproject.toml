[project]
name = "ece143-llm-impact"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.0",
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "nbstripout>=0.9.0",
]
